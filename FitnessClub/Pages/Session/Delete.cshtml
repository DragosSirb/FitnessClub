@page
@model FitnessClub.Pages.Session.DeleteModel
@{
    ViewData["Title"] = "Delete";
}

<link href="~/css/crud.css" rel="stylesheet" />

<div class="container mt-5">
    <h1 class="text-center">Delete Session</h1>
    <h3 class="text-center">Are you sure you want to delete this session?</h3>

    <div class="card">
        <h4 class="card-title text-center">Session Details</h4>
        <hr />
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Sessions.Date)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Sessions.Date)
                </dd>
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Sessions.Location)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Sessions.Location)
                </dd>
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Sessions.MemberId)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Sessions.MemberId)
                </dd>
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Sessions.TrainerId)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Sessions.TrainerId)
                </dd>
            </dl>
        </div>
    </div>

    <form method="post" class="text-center mt-4">
        <input type="hidden" asp-for="Sessions.Id" />
        <button type="submit" class="btn btn-danger btn-lg">Delete</button>
        <br />
        <a class="btn btn-link mt-3" asp-page="./Index">Back to List</a>
    </form>
</div>
