@page
@model FitnessClub.Pages.Recommandation.EditModel

@{
    ViewData["Title"] = "Edit";
}

<link rel="stylesheet" href="~/css/crud.css" />

<div class="container mt-5">
    <div class="card p-4">
        <h1>Edit Recommendation</h1>
        <h4>Update the recommendation details</h4>
        <hr />

        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Recommendations.Id" />

            <div class="form-group">
                <label asp-for="Recommendations.Description" class="control-label">Description</label>
                <textarea asp-for="Recommendations.Description" class="form-control"></textarea>
                <span asp-validation-for="Recommendations.Description" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Recommendations.Category" class="control-label">Category</label>
                <input asp-for="Recommendations.Category" class="form-control" />
                <span asp-validation-for="Recommendations.Category" class="text-danger"></span>
            </div>


            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>

        <div class="mt-3">
            <a class="btn btn-secondary" asp-page="./Index">Back to List</a>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
